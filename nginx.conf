upstream api_cluster {
    server web_replica_1:8000;
    server web_replica_2:8000;
    server web_replica_3:8000;
}

server {
    listen 80;

    location / {
        proxy_pass http://api_cluster;
        proxy_connect_timeout 2s;
        proxy_read_timeout 2s;
    }
}
